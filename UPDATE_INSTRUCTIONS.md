# GitHub profile update

Repository:
https://github.com/mohaslam11/mohaslam11

## Files to copy

Replace the repository contents with:

- `README.md`
- `assets/header.png`
- `assets/footer.png`
- `.github/workflows/snake.yml`

## Important

1. Keep the repository name exactly `mohaslam11`.
2. The repository must be public.
3. Commit/push all four items to the `main` branch.
4. Open GitHub → Actions → `generate contribution snake` → run it manually once.
5. After the workflow succeeds, refresh the profile README.
6. In your GitHub profile, update the repository description to:
   `Software Developer building mobile, full-stack and AI products.`

## Pinned repositories

After updating the README, pin these repositories in this order:

1. `medical-ai`
2. `platesy-food-sharing`
3. `travnook-lead-qualifier`
4. `Mohammed-Aslam-personal-portfolio`
5. `Smartpath-gulf` (if this is the current public repository)
6. Keep other repositories below these.

## Profile bio

Suggested GitHub bio:

Software Developer | Flutter • Full-Stack • AI Integration

## Why this version

The README removes the fragile third-party visual services used by the previous version:
- Capsule Render
- Readme Typing SVG
- GitHub Readme Stats
- Streak Stats
- Profile Trophy
- Shields badge wall

The header/footer are stored inside the repository itself. The only generated visual retained is the contribution snake, which is generated by GitHub Actions into the repository's `output` branch.
